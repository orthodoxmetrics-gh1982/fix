# <%= name %>

**Type**: <%= type.charAt(0).toUpperCase() + type.slice(1) %>  
**Route**: `<%= route %>`  
**DB Table**: `<%= dbTable %>`  
**Roles**: `<%= JSON.stringify(roles) %>`  
**Description**: <%= description %>

---

✅ Linked from dashboard  
📡 Binds to `GET <%= route %>`

## 🔍 Analysis Results

### routeChecker
✅ Route <%= route %> is live and accessible

### databaseChecker
✅ Database table '<%= dbTable %>' exists

### securityAnalyzer
🔒 Security Analysis:
✅ No security issues detected

## 📋 Metadata

**Created**: <%= metadata.createdAt %>  
**Updated**: <%= metadata.updatedAt %>  
**Created By**: <%= metadata.createdBy %>

## 💻 Usage

This component is automatically generated and managed by the OMB Visual Editor.

### API Integration
- **Endpoint**: `<%= route %>`
- **Method**: GET
- **Authentication**: Required
- **Roles**: <%= roles.join(', ') %>

### Database Schema
- **Table**: `<%= dbTable %>`
- **Access**: <%= roles.includes('admin') ? 'Admin only' : 'Role-based' %>

## 🚀 Generated Files

This component generates the following files:

- **Component**: `src/pages/auto/<%= id %>.tsx`
- **API Route**: `src/api/auto/<%= id %>.ts`
- **Documentation**: `docs/OM-BigBook/pages/components/<%= id %>.md`

## 🔧 Development

### Component File
```typescript
// src/pages/auto/<%= id %>.tsx
export default function <%= name.replace(/[^a-zA-Z0-9]/g, '') %>() {
  // Auto-generated component code
}
```

### API Route
```typescript
// src/api/auto/<%= id %>.ts
router.get('<%= route %>', async (req, res) => {
  // Auto-generated API code
});
```

## 📊 Statistics

- **Generated**: <%= new Date().toISOString() %>
- **Component Type**: <%= type %>
- **Access Roles**: <%= roles.length %> roles
- **Database Table**: <%= dbTable %>

---

*This documentation is automatically maintained by the OM-AI system.* 